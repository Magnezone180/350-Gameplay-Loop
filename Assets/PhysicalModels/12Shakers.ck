Shakers shak => dac;

17 => shak.preset; // coins in a mug

while( true )
{
    Math.random2f(50.0, 128.0) => shak.objects;
    Math.random2f(0.0, 1.0) => shak.decay;
    1.0 => shak.energy;  // not .noteOn
    1.0::second => now;
}


